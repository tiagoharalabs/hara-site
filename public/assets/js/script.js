(()=>{const header=document.querySelector('.site-header'),btn=document.querySelector('.menu-button'),menu=document.querySelector('.mobile-menu');const head=()=>header?.classList.toggle('scrolled',scrollY>8);head();addEventListener('scroll',head,{passive:true});btn?.addEventListener('click',()=>{const open=btn.getAttribute('aria-expanded')==='true';btn.setAttribute('aria-expanded',String(!open));menu?.classList.toggle('open',!open);menu?.setAttribute('aria-hidden',String(open));});menu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{btn?.setAttribute('aria-expanded','false');menu.classList.remove('open');menu.setAttribute('aria-hidden','true')}));const els=document.querySelectorAll('.reveal');if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in-view');io.unobserve(e.target)}}),{threshold:.1});els.forEach(e=>io.observe(e))}else els.forEach(e=>e.classList.add('in-view'))})();